# 변경 요약

## v2 보강판
- 보안: CSP 추가, 외부 JS/CSS 분리, SVG sanitize 적용
- 편의성: 확대 허용, 라이트/다크 테마, 페이지 점프 추가
- 안정성: 파일 형식/크기 검사, 문서 교체 시 메모리 정리, 서비스워커 캐시 버전 갱신
- 표현 수정: `서버 전송 없음` 대신 `파일 업로드 없음 · 브라우저 안에서 처리`로 정리

## 남는 한계
- HWP/HWPX 파싱 안전성의 상당 부분은 `rhwp.js`/`rhwp_bg.wasm` 엔진 자체 품질에 의존
- 브라우저에서 직접 악성 문서를 완전 무해화하는 보장은 어려움
- 공개 호스팅은 데모/개인 사용엔 가능하지만, 매우 민감한 문서 실사용엔 신중 권장
